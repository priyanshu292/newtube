import React from 'react'
import VideoContainer from './VideoContainer'

const MainContainer = () => {
  return (
    <div className=''>
      <VideoContainer />
    </div>
  )
}

export default MainContainer
